plugins {
    id 'java'
}

// TODO: uncomment to be able to fetch packages in your own repository
repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/Ingsis-Group-4/printscript")
        credentials {
            username = project.findProperty("gpr.user") ?: System.getenv("USERNAME")
            password = project.findProperty("gpr.key") ?: System.getenv("TOKEN")
        }
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "7m" // maximum heap size
}

dependencies {
    testImplementation 'junit:junit:4.13.1'
    implementation 'printscript:common:0.0.1-SNAPSHOT'
    implementation 'printscript:interpreter:0.0.1-SNAPSHOT'
    implementation 'printscript:parser:0.0.1-SNAPSHOT'
    implementation 'printscript:lexer:0.0.1-SNAPSHOT'
    implementation 'printscript:utils:0.0.1-SNAPSHOT'
}

test {
    useJUnit()
}
