plugins {
    id 'java'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/G16IS/printscript")
        credentials {
            username = project.findProperty("gpr.user") ?: System.getenv("USERNAME")
            password = project.findProperty("gpr.key") ?: System.getenv("TOKEN")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "22m" // maximum heap size
}
def version = "pepe1"
dependencies {
    testImplementation 'junit:junit:4.13.1'
    implementation("com.g16is.printscript:interpreter:$version")
    implementation("com.g16is.printscript:application:$version")
    implementation("com.g16is.printscript:common:$version")
    implementation("com.g16is.printscript:infrastructure:$version")

}

test {
    environment "BEST_FOOTBALL_CLUB", "San Lorenzo"
    useJUnit()
}
