plugins {
    id 'java'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/Agustin5421/PrintScript")
        credentials {
            username = project.findProperty("gpr.user") ?: System.getenv("USERNAME")
            password = project.findProperty("gpr.key") ?: System.getenv("TOKEN")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "7m" // maximum heap size
}

dependencies {
    testImplementation 'junit:junit:4.13.1'
    implementation 'com.google.code.gson:gson:2.8.9'
    implementation 'com.ingsis:lexer:1.1.3'
    implementation 'com.ingsis:parser:1.1.3'
    implementation 'com.ingsis:interpreter:1.1.3'
    implementation 'com.ingsis:formatter:1.1.3'
    implementation 'com.ingsis:linter:1.1.3'
    implementation 'com.ingsis:runner:1.1.3'
    implementation 'com.ingsis:utils:1.1.3'
}

test {
    environment "BEST_FOOTBALL_CLUB", "San Lorenzo"
    useJUnit()
}
