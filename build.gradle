plugins {
    id 'java'
}

// TODO: uncomment to be able to fetch packages in your own repository
repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = 'GitHubPackages'
        url = uri("https://maven.pkg.github.com/chanflito/printscript")
        credentials {
            username = System.getenv('GITHUB_ACTOR')
            password = System.getenv('GITHUB_TOKEN')
        }
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "9m" // maximum heap size
}

dependencies {
    implementation 'PrintScript:lexer:1.1.19-SNAPSHOT'
    implementation 'PrintScript:interpreter:1.1.19-SNAPSHOT'
    implementation 'PrintScript:parser:1.1.19-SNAPSHOT'
    implementation 'PrintScript:common:1.1.19-SNAPSHOT'
    implementation 'PrintScript:iterator:1.1.19-SNAPSHOT'
    testImplementation 'junit:junit:4.13.1'
}

test {
    useJUnit()
}
