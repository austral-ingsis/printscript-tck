plugins {
    id 'java'
}

// TODO: uncomment to be able to fetch packages in your own repository
repositories {
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/Bautista-Baiocchi-lora/PrintScript")
        credentials {
            username = "Bautista-Baiocchi-lora"//project.findProperty("gpr.user") ?: System.getenv("USERNAME")
            password = "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"//project.findProperty("gpr.key") ?: System.getenv("TOKEN")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(15)
    }
}

dependencies {
    testImplementation 'junit:junit:4.13'
    implementation 'org.example:cli:1.1.13'
}

test {
    useJUnit()
}
