plugins {
    id 'java'
    id "com.github.eugenesy.scapegoat" version "0.2.0"

}

repositories {
    gradlePluginPortal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/flor-resoagli/PrintScript")
        credentials {
//            find properties on gradle.properties file
            username = 'IsabelaCeriani'
            password = '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'
//            username = System.getProperty("USERNAME")
//            password = System.getProperty("TOKEN")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(16)
    }
}

dependencies {
    testImplementation 'junit:junit:4.13.2'
    implementation 'org.florresoagli:cli:0.0.9'
    implementation 'org.florresoagli:parser:0.0.9'
    implementation 'org.florresoagli:lexer:0.0.9'


}

test {
    useJUnit()
}

repositories {
    mavenCentral()
}
