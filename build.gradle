plugins {
    id 'java'
}

// TODO: uncomment to be able to fetch packages in your own repository
repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/SantiPe02/printscript")
        credentials {
            username = project.findProperty("GITHUB_ACTOR") ?: System.getenv("USERNAME")
            password = project.findProperty("GITHUB_TOKEN") ?: System.getenv("TOKEN")
        }
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "7m" // maximum heap size
}

dependencies {
    testImplementation 'junit:junit:4.13.1'
    implementation 'com.github.SantiPe02:ast:1.0.0-SNAPSHOT'
    implementation 'com.github.SantiPe02:interpreter:1.0.0-SNAPSHOT'
    implementation 'com.github.SantiPe02:lexer:1.0.0-SNAPSHOT'
    implementation 'com.github.SantiPe02:parser:1.0.0-SNAPSHOT'
    implementation 'com.github.SantiPe02:util:1.0.0-SNAPSHOT'
    implementation 'com.github.SantiPe02:token:1.0.0-SNAPSHOT'
}

test {
    useJUnit()
}
