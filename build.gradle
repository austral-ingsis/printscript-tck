plugins {
    id 'java'
}

// TODO: uncomment to be able to fetch packages in your own repository
repositories {
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/nickyfox/IngSisParser")
        credentials {
            username = project.findProperty("gpr.user") ?: "nickyfox"
            password = project.findProperty("gpr.key") ?: "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"
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(15)
    }
}

dependencies {
    testImplementation 'junit:junit:4.13'
    implementation 'com.printscript.ingsis:cli:1.0.23'
}

test {
    useJUnit()
}
