plugins {
    id 'java'
}

// TODO: uncomment to be able to fetch packages in your own repository
repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/Pedrodeforonda/printScript-ingsis")
        credentials {
            username = project.findProperty("gpr.user") ?: System.getenv("USERNAME")
            password = project.findProperty("gpr.key") ?: System.getenv("TOKEN")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "7m" // maximum heap size
}

dependencies {
    testImplementation 'junit:junit:4.13.1'
    implementation("com.github.printSrcript:token:1.0.33")
    implementation("com.github.printSrcript:ast:1.0.33")
    implementation("com.github.printSrcript:lexer:1.0.33")
    implementation("com.github.printSrcript:parser:1.0.33")
    implementation("com.github.printSrcript:interpreter:1.0.33")
    implementation("com.github.printSrcript:formatter:1.0.33")
    implementation("com.github.printSrcript:linter:1.0.33")

}

test {
    environment "BEST_FOOTBALL_CLUB", "San Lorenzo"
    useJUnit()
}

