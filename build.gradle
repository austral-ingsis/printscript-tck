plugins {
    id 'java'
}

repositories {
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/sofiasdz/TpIngSis")
        credentials {
            username = project.findProperty("gpr.user") ?: "sofiasdz"
            password = project.findProperty("gpr.key") ?: "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"

        }
    }
}

dependencies {
    implementation 'TpIngSis:parser:2.3-SNAPSHOT'
    testImplementation 'junit:junit:4.13'
}

test {
    useJUnit()
}
sourceCompatibility = 14

tasks.withType(JavaCompile) {
    options.compilerArgs += '--enable-preview'
}
tasks.withType(Test) {
    jvmArgs += "--enable-preview"
}