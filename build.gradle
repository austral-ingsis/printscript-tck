plugins {
    id 'java'
}


repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = 'GitHubPackages'
        url = uri("https://maven.pkg.github.com/ingsis-group-12/printscript")
        credentials {
            username = project.findProperty('gpr.user') ?: System.getenv('GITHUB_ACTOR')
            password = project.findProperty('gpr.key') ?: System.getenv('GITHUB_TOKEN')
        }
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "9m" // maximum heap size
}

dependencies {
    implementation 'PrintScript:lexer:1.1.20-SNAPSHOT'
    implementation 'PrintScript:interpreter:1.1.20-SNAPSHOT'
    implementation 'PrintScript:parser:1.1.20-SNAPSHOT'
    implementation 'PrintScript:common:1.1.20-SNAPSHOT'
    implementation 'PrintScript:iterator:1.1.20-SNAPSHOT'
    testImplementation 'junit:junit:4.13.1'
}

test {
    useJUnit()
}
